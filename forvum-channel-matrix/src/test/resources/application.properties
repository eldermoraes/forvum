# Pin forvum.home to a path with no channels/matrix.json so the @QuarkusTest IT exercises the
# unconfigured/inert posture deterministically (MatrixChannel.onStart warns + no-ops; no sync loop),
# regardless of the developer's real ~/.forvum/.
forvum.home=${java.io.tmpdir}/forvum-matrix-it-home-absent
