# Pin forvum.home to a path with no memory/qdrant.json so the @QuarkusTest wiring IT exercises the
# unconfigured/inert posture deterministically, regardless of the developer's real ~/.forvum/.
forvum.home=${java.io.tmpdir}/forvum-qdrant-it-home-absent
