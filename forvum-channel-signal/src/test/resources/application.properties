# Pin forvum.home to a path with no channels/signal.json so the @QuarkusTest IT exercises the
# inert/unconfigured posture deterministically, regardless of the developer's real ~/.forvum/.
forvum.home=${java.io.tmpdir}/forvum-signal-it-home-absent
