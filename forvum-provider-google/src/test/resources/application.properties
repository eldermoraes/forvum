# Test-only config: the quarkus-langchain4j-ai-gemini extension requires this property at startup
# (unlike the OpenAI extension, which accepts an empty default). A placeholder satisfies the
# extension's startup validation; no live Google service is called in the contract tests.
quarkus.langchain4j.ai.gemini.api-key=test-placeholder
