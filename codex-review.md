- **[CRITICAL][Security] The approval and CAPR dashboards require neither authentication nor authorization.** Any client with HTTP access can list tool calls and their arguments, approve or reject pending executions, and query CAPR records with `sessionId`, `agentId`, and justifications. The code itself declares the absence of authentication, and the E2E test consolidates `200` responses without credentials. The risk is amplified by the likewise-unauthenticated WebSocket, the bind on `0.0.0.0`, and the exposure of the service inside the cluster. Evidence: `forvum-app/src/main/java/ai/forvum/app/ApprovalDashboardRoute.java:34-64`, `forvum-app/src/main/java/ai/forvum/app/CaprDashboardRoute.java:45-51`, `forvum-channel-web/src/main/java/ai/forvum/channel/web/ChatSocket.java:36-52`, `forvum-app/src/test/java/ai/forvum/e2e/ApprovalDashboardE2E.java:50-77`, `deploy/helm/forvum/templates/deployment.yaml:53-57`, `deploy/helm/forvum/templates/service.yaml:11-18`.

- **[HIGH][Security] Device pairing does not authenticate the device.** The turn flow only checks whether any device exists for the channel; the `token`, described as a shared secret, is neither presented nor validated in production, and `approvedScopes` is explicitly stored without restricting the turn's capabilities. A single file such as `devices/web.json` authorizes every connection on that channel. Evidence: `forvum-engine/src/main/java/ai/forvum/engine/agent/TurnService.java:124-135`, `forvum-engine/src/main/java/ai/forvum/engine/pairing/DeviceRegistry.java:75-99`, `forvum-engine/src/main/java/ai/forvum/engine/pairing/Device.java:13-28`; references to `token()` and scope coverage are limited to parsing/existence tests.

- **[HIGH][Security & alignment] The privilege ceiling defined in `AgentSpec.roles` is not enforced.** The spec dictates intersecting the caller's scopes with the agent's roles, but `TurnService` derives scopes solely from identity and `CronScheduler` uses fixed scopes; neither consults the persona's roles. As a result, configuring an agent with restricted roles produces a false sense of isolation. Evidence: `docs/ULTRAPLAN.md:1349`, `docs/design-rounds/group-8-security-identity-multiuser.md:256-269`, `forvum-engine/src/main/java/ai/forvum/engine/agent/AgentSpecReader.java:75-80`, `forvum-engine/src/main/java/ai/forvum/engine/agent/TurnService.java:145-160`, `forvum-engine/src/main/java/ai/forvum/engine/cron/CronScheduler.java:159-169`.

- **[HIGH][Security & alignment] The identity fallback declared in `AgentSpec.identityId` is not used.** For unresolved users, `TurnService` always uses `anonymous`; since identities without roles receive `RoleRegistry`'s permissive default set, the security fallback foreseen by the spec is ignored. The tests verify only parsing and inheritance of the field. Evidence: `docs/ULTRAPLAN.md:1350`, `forvum-engine/src/main/java/ai/forvum/engine/agent/AgentSpecReader.java:77-81`, `forvum-engine/src/main/java/ai/forvum/engine/agent/TurnService.java:134-148`, `forvum-engine/src/main/java/ai/forvum/engine/agent/RoleRegistry.java:28-45`, `forvum-engine/src/main/java/ai/forvum/engine/agent/RoleRegistry.java:70-72`.

- **[HIGH][Security, cost & alignment] `costBudget` and `toolBudget` are not enforced at runtime.** `costBudget` is discarded by the spec reader, the spawn budget guard remains a `TODO`, and the meter does not participate in the normal flow; `toolBudget` is read but not consumed by the graph, which uses only a fixed round limit. This allows spend and tool calls beyond the declared limits and contradicts the hard-stop documented by `BudgetExhaustedException`. Evidence: `docs/ULTRAPLAN.md:1304-1318`, `docs/ULTRAPLAN.md:1347`, `docs/CONTEXT-ENGINEERING-MAPPING.md:24`, `forvum-engine/src/main/java/ai/forvum/engine/agent/AgentSpecReader.java:27-28`, `forvum-engine/src/main/java/ai/forvum/engine/agent/AgentSpecReader.java:62-81`, `forvum-engine/src/main/java/ai/forvum/engine/agent/AgentRegistry.java:92-95`.

- **[HIGH][Security] The channel, pairing, and RBAC defaults form a complete permissive path.** Empty allowed-user lists accept any sender on several remote channels; without device files, pairing is disabled; an unresolved identity receives all of the default role's scopes. If the agent's tool belt contains sensitive tools, any platform user can consume the model and trigger those capabilities. Evidence: `forvum-channel-telegram/src/main/java/ai/forvum/channel/telegram/TelegramChannelConfig.java:115-130`, `forvum-channel-discord/src/main/java/ai/forvum/channel/discord/DiscordChannelConfig.java:117-132`, `forvum-channel-whatsapp/src/main/java/ai/forvum/channel/whatsapp/WhatsAppChannelConfig.java:135-154`, `forvum-engine/src/main/java/ai/forvum/engine/pairing/DeviceRegistry.java:24-27`, `forvum-engine/src/main/java/ai/forvum/engine/pairing/DeviceRegistry.java:75-77`, `forvum-engine/src/main/java/ai/forvum/engine/agent/RoleRegistry.java:28-45`.

- **[HIGH][Supply-chain security] The JVM plugin resolver does not implement the documented integrity and permission controls.** Maven Resolver is started without an explicit strict-checksum policy, while directories and JARs are created honoring only the `umask`, without guaranteeing `0700`/`0600`. Since installed plugins execute arbitrary code in the same process, a corrupted or substituted artifact inherits the full authority of the application. Evidence: `forvum-engine/src/main/java/ai/forvum/engine/plugin/MavenPluginResolver.java:79-98`, `forvum-engine/src/main/java/ai/forvum/engine/plugin/MavenPluginResolver.java:121-128`, `forvum-engine/src/main/java/ai/forvum/engine/plugin/MavenPluginResolver.java:145-147`, `docs/ULTRAPLAN.md:1924`, `docs/ISSUES.md:1356`.

- **[MEDIUM][Security] Error responses bypass the `OutputGuard`.** The redactor is applied to the successful response, but exceptions are converted into an `ErrorEvent` carrying the message and deepest cause and sent directly through the channels. Provider or tool errors can therefore expose secrets, URLs, PII, or response content without passing through the output policy. The tests cover redaction only on the success path and require only a non-null message on the error path. Evidence: `forvum-engine/src/main/java/ai/forvum/engine/agent/TurnService.java:164-188`, `forvum-engine/src/main/java/ai/forvum/engine/agent/TurnService.java:208-227`, `forvum-channel-web/src/main/java/ai/forvum/channel/web/ChatSocket.java:71-79`, `forvum-engine/src/test/java/ai/forvum/engine/agent/OutputGuardRedactionIT.java`, `forvum-engine/src/test/java/ai/forvum/engine/agent/TurnServiceErrorIT.java`.

- **[MEDIUM][Data-at-rest security] The state directory created on first boot does not receive restrictive permissions.** `Files.createDirectories` leaves protection of the SQLite store, ledger, tool arguments, and persisted errors dependent on the `umask`; the guideline itself acknowledges the `0700` gap in the no-`init` flow. Evidence: `forvum-engine/src/main/java/ai/forvum/engine/persistence/StateDirInitializer.java:31-38`, `docs/ULTRAPLAN.md:1940`.

- **[MEDIUM][Supply-chain security] CI has no automated gate for vulnerabilities, secrets, SAST, SBOM, or provenance.** The workflows cover build, QA, evaluation, Helm, and release, but there is no Dependency Review/OWASP/OSV, CodeQL, Trivy, gitleaks, CycloneDX, or attestation; the checksum published alongside the binary does not provide signed provenance. No specific CVE was inferred: the finding is the absence of the preventive and detective controls. Evidence: `.github/workflows/`, `pom.xml`, `docs/ULTRAPLAN.md:1924-1950`.

- **[HIGH][Context Engineering] Local semantic/episodic memory does not participate in the normal turn write-and-select cycle.** `AgentMemory.recordTurn` persists messages and a generic event, but `recordFact` has no production caller; `MemorySelector` selects only one `MemoryProvider`, and the only bundled provider is Qdrant, which returns empty when unconfigured. The SQLite semantic table is queried by the CLI, not by the turn. Therefore, the default installation neither extracts long-term facts nor retrieves semantic/episodic memory in the next conversation, at odds with the three-tier mapping. Evidence: `docs/CONTEXT-ENGINEERING-MAPPING.md:34-45`, `forvum-engine/src/main/java/ai/forvum/engine/agent/Agent.java:103-118`, `forvum-engine/src/main/java/ai/forvum/engine/agent/AgentMemory.java:51-56`, `forvum-engine/src/main/java/ai/forvum/engine/agent/AgentMemory.java:86-111`, `forvum-engine/src/main/java/ai/forvum/engine/routing/MemorySelector.java:41-64`, `forvum-provider-memory-qdrant/src/main/java/ai/forvum/provider/memory/qdrant/QdrantMemoryProvider.java:64-73`.

- **[MEDIUM][Context Engineering] Compression fails open and reinserts raw data into the context.** If the summarizer fails or returns empty content, the graph uses the retrieved memory or the worker digest without compression and without a fallback ceiling. This contradicts the guarantee that only a compressed digest crosses the boundary and allows context overflow/contamination precisely during summarization-model unavailability. Evidence: `docs/CONTEXT-ENGINEERING-MAPPING.md:54-68`, `forvum-engine/src/main/java/ai/forvum/engine/graph/SupervisorGraph.java:189-217`, `forvum-engine/src/main/java/ai/forvum/engine/graph/SupervisorGraph.java:494-510`.

- **[MEDIUM][Context Engineering & availability] Workers created by `Spawn` are not discarded after execution.** Model-suggested IDs are registered via `putIfAbsent`, there is no removal in the worker lifecycle, and a later reuse of the same ID fails; unique IDs accumulate agents and in-memory state indefinitely. This weakens the ephemeral-isolation semantics and creates a resource-exhaustion vector. Evidence: `forvum-engine/src/main/java/ai/forvum/engine/graph/SupervisorGraph.java:434-453`, `forvum-engine/src/main/java/ai/forvum/engine/agent/AgentRegistry.java:97-123`, `forvum-engine/src/main/java/ai/forvum/engine/graph/DefaultWorkerRunner.java:38-52`.

- **[MEDIUM][Alignment & concurrency] Agent hot reload is not safe for in-flight turns and may keep stale tool belts.** The registry removes the spec without destroying the agent context and admits that removal during a turn can cause failure; `AgentToolBelt` memoizes the filtered list for the whole bean lifecycle. After a change, the persona may disappear from an active turn or keep using old capabilities, contradicting the documented dynamic-configuration behavior. Evidence: `forvum-engine/src/main/java/ai/forvum/engine/agent/AgentRegistry.java:145-170`, `forvum-engine/src/main/java/ai/forvum/engine/agent/AgentToolBelt.java:27-43`, `forvum-engine/src/test/java/ai/forvum/engine/agent/AgentRegistryTest.java:92-118`.

- **[MEDIUM][Alignment] The architecture and status documentation is materially divergent from the code.** The Context Engineering mapping still says only M1 was delivered, while the README declares phase 2/v0.5 complete; `CLAUDE.md` states in one passage that JaCoCo is not configured and in another that it is; the mapping also declares a budget hard-stop, a three-tier memory cycle, and only-compressed-digest crossing, although the corresponding flows are absent or fail-open. Since these documents are treated as the source of truth, the divergence prevents safely assessing conformance and evolution. Evidence: `docs/CONTEXT-ENGINEERING-MAPPING.md:3`, `docs/CONTEXT-ENGINEERING-MAPPING.md:24-26`, `docs/CONTEXT-ENGINEERING-MAPPING.md:45-68`, `docs/CONTEXT-ENGINEERING-MAPPING.md:115-117`, `README.md`, `CLAUDE.md:309-312`, `CLAUDE.md:1015-1031`.

- **[MEDIUM][Test coverage] The global 80%-line / 75%-branch gate is weakened by exclusions and lower local thresholds.** The engine module excludes classes with relevant logic — compaction, summarization, task recording, replay, semantic query/memory, and evaluation — and not just DTOs or bootstrap; the app lowers branches to 70% and Telegram lowers lines to 72%. Thus, `mvn verify` can pass even with regressions below the declared quality bar. Evidence: `pom.xml:72-155`, `forvum-engine/pom.xml:167-195`, `forvum-app/pom.xml:250-283`, `forvum-channel-telegram/pom.xml:70-103`, `docs/ULTRAPLAN.md:1950`.

- **[MEDIUM][Test coverage] The live provider, browser, and sandbox tests are not run by the observed CI.** The app's Surefire excludes the `live` tag; the `native-turn` job configures only Failsafe groups, while the scripted Anthropic/OpenAI/Google/Ollama tests are `*E2E` classes run by Surefire. `BrowserLiveIT` and `SandboxRunLiveTest` are likewise opt-in, and there is no nightly workflow referencing them, despite the guideline announcing periodic live tests. Evidence: `forvum-app/pom.xml:18-36`, `.github/workflows/ci.yml:185-190`, `forvum-app/src/test/java/ai/forvum/e2e/AnthropicScriptedTurnE2E.java`, `forvum-app/src/test/java/ai/forvum/e2e/OpenAiScriptedTurnE2E.java`, `forvum-app/src/test/java/ai/forvum/e2e/GoogleScriptedTurnE2E.java`, `forvum-tools-browser/src/test/java/ai/forvum/tools/browser/BrowserLiveIT.java`, `forvum-tools-sandbox/src/test/java/ai/forvum/tools/sandbox/SandboxRunLiveTest.java`.

- **[MEDIUM][Test coverage] The mutation testing promised in the guidelines does not exist in the executable configuration.** There is no Pitest plugin in the POMs nor a workflow producing the mutation-score signal described in the plan; therefore, the suite measures line/branch execution but does not detect tests that pass without validating behavior. Evidence: `docs/ULTRAPLAN.md:1950`, `CLAUDE.md`, `pom.xml`, `.github/workflows/`.

- **[HIGH][Security test coverage] The tests verify mainly parsing/structure, not the enforcement of the declared critical controls.** There is no E2E test that authenticates a device token and restricts `approvedScopes`, enforces `AgentSpec.roles`/`identityId`, halts on `costBudget`/`toolBudget`, protects the dashboards, redacts errors, or proves long-term memory in a normal conversation. The prompt-injection test uses an empty tool belt, so it demonstrates only that the prompt does not create a nonexistent capability, not that a legitimate, sensitive tool stays protected under injection. Evidence: `forvum-engine/src/test/java/ai/forvum/engine/agent/AgentSpecReaderTest.java`, `forvum-engine/src/test/java/ai/forvum/engine/pairing/DeviceRegistryTest.java`, `forvum-app/src/test/java/ai/forvum/security/PromptInjectionToolDeniedTest.java:31-78`, `forvum-app/src/test/java/ai/forvum/e2e/ApprovalDashboardE2E.java:50-77`.
