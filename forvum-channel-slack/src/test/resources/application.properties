# Pin forvum.home to a hermetic, ABSENT path for this module's @QuarkusTest boot: SlackChannel.onStart
# runs at boot and must exercise the inert no-config contract (warn + no-op, no apps.connections.open,
# no socket) regardless of the developer's real ~/.forvum/channels/slack.json.
forvum.home=target/slack-test-absent-forvum-home
