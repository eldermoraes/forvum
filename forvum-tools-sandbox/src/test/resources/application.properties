# Pin forvum.home to a path with no tools/sandbox.json so the @QuarkusTest wiring IT exercises the
# fail-closed/inert posture deterministically, regardless of the developer's real ~/.forvum/.
forvum.home=${java.io.tmpdir}/forvum-sandbox-it-home-absent
